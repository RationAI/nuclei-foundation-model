defaults:
  - hydra: default
  - logger: mlflow
  - _self_

seed: 0
mode: ???
checkpoint: null

trainer:
  max_epochs: 100
  precision: 32-true
  gradient_clip_val: 0.1

model:
  ema_momentum: 0.999
  dino:
    num_prototypes: 65536
    bottleneck_dim: 256
    num_layers: 3
    hidden_dim: 2048
  ibot:
    num_prototypes: 65536
    bottleneck_dim: 256
    num_layers: 3
    hidden_dim: 2048

metadata:
  experiment_name: Nuclei Foundational Model
  run_name: ???
  description: ???
  hyperparams: {}
