# @package _global_

data:
  batch_size: 10
  num_workers: 6
  train:
    _target_: nfm.data.datasets.NucleiDataset
    slides_path: s3://nuclei/slides_cropped.parquet
    nuclei_path: s3://nuclei

metadata:
  run_name: Training
  description: Training of the model on full data
