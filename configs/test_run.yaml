defaults:
  - /default.yaml  # or relative path to your default config
  - /data/datasets@data.train: train
  - /data/datasets@data.val: val
  - _self_

logger: null

mode: fit

trainer:
  limit_val_batches: 0

data:
  batch_size: 1
  num_workers: 0

